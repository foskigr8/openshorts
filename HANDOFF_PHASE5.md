# Superseded — see MASTER_PLAN_FRAMING_REBUILD.md

This document was the original Phase 5 handoff. It has been folded into
`MASTER_PLAN_FRAMING_REBUILD.md` at the repo root, which is now the single,
more complete entry point — it includes everything here plus the Kaggle
download-failure diagnosis, the `/app` cookies path bug, and the notebook
gaps found while trying to validate this work on Kaggle.

Read `MASTER_PLAN_FRAMING_REBUILD.md` instead. This file is kept only so old
links/references don't 404.
