scenedetect==0.7
transnetv2-pytorch==1.0.5
ultralytics==8.4.46
torch==2.11.0
torchvision==0.26.0
tqdm==4.67.3
yt-dlp
faster-whisper==1.2.1
py3langid==0.3.0
google-genai==1.75.0
python-dotenv==1.2.2
mediapipe==0.10.14
boto3==1.43.4

fastapi==0.136.1
uvicorn==0.46.0
python-multipart==0.0.27
httpx==0.28.1
Pillow==12.2.0
beautifulsoup4==4.14.3
# Voice isolation for the 'remove background audio' option (audio_cleanup.py).
# Only imported when that option is used; absent -> the FFmpeg denoise fallback.
demucs==4.1.0
# Linear-assignment solver required by ultralytics' ByteTrack/BoT-SORT
# trackers (already vendored in ultralytics; it just won't import without this).
lap==0.5.13
# MFCC features for LR-ASD active-speaker detection (asd_worker.py).
python_speech_features==0.6
# Test runner. In the image on purpose: the source tree is bind-mounted at
# /app (see docker-compose.yml), so code changes survive a restart — but a
# rebuilt container came up with no pytest, so `python3 -m pytest tests/`
# failed and the suite silently stopped being runnable after a restart.
pytest==8.3.4
# Persistent clip storage on HuggingFace Hub (hf_storage.py). Kaggle wipes
# /kaggle/working at session end, so clips rendered there evaporate without
# a backup; HF was chosen over R2/B2 because it needs no credit card.
huggingface_hub>=0.23
# Named-speaker enrichment (face_id.py): ArcFace/buffalo_l face recognition,
# used to turn anonymous diarized labels ("SPEAKER_00") into real names before
# clip selection reads the transcript. InsightFace over DeepFace/dlib on the
# integration guide's recommendation: ~15ms/face on a T4 vs ~80ms and ~300ms,
# and it shares the ONNX ecosystem YOLOv8 already uses.
#
# MEASURED 6-aug-2026 on the Kaggle host: installing this unconstrained pulls
# opencv 5.x through albumentations -> albucore, and opencv 5 is what drags a
# numpy major-version jump in behind it. Capping opencv is the precise fix.
#
# Pinning numpy instead is NOT: Kaggle ships numpy 2.0.2 and mediapipe 0.10.14
# resolves against it perfectly well, so a `numpy<2` line there is
# unsatisfiable and takes the ENTIRE install down with it (ResolutionImpossible
# -> no yt-dlp, no ultralytics, no mediapipe). That was a real outage, not a
# hypothetical.
insightface==0.7.3
# Not imported anywhere — guards, per the note above.
opencv-python<5
opencv-python-headless<5
#
# insightface also needs an ONNX RUNTIME, deliberately NOT listed here:
# onnxruntime-gpu is ~2GB and lives in the Dockerfile's GPU block (alongside
# onnx-asr for Parakeet) so the default CPU image stays slim. GPU images and
# kaggle_bootstrap.sh install it. Without one, face_id fails open and the
# pipeline runs with anonymous speakers exactly as before.
